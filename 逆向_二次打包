1. Java 代码中加入签名校验(直接修改smali文件绕过)
2. NDK 中加入签名校验(ida查看直接hex修改就可以绕过)
3. 利用二次打包工具本身的缺陷阻止打包(manifest 欺骗，图片修改后缀等等)
4. 登录时服务器发送随机密钥，利用ndk实时计算文件签名，并使用获得的密钥加密，然后上报到服务器，由服务器校验签名是否有效，
    决定是否允许该软件登录。核心业务逻辑放服务器上，不允许绕过服务器单机运行。
